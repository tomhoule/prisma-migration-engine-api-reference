//! The $METHOD_NAME migration engine API method.
//!
$(
  echo $METHOD | jq -r '.description' | \
  while read line; do
    echo "//! $line"
  done
)

#[derive(serde::Deserialize, Debug)]
pub struct Request {
$(
  echo $METHOD | jq -c '.requestShape[] | .[]'
)
}

#[derive(serde::Serialize, Debug)]
pub struct Response {
}
